# CoffeeCall Code Analysis Plan

## Objective
Identify all hardcoded values (strings, numbers, colors) that should be moved to appropriate constants files.

## Scope
- Scan all Swift files in `/Users/development/Documents/CoffeeCall/apps/frontend/Coffee_Call/`
- Check against existing constants:
  - AppStrings.swift for string constants
  - AppConstants.swift for layout/radar/animation constants  
  - Color+Extensions.swift for semantic color tokens
  - Font+Extensions.swift for font tokens
  - Design/design-tokens.md for design system reference

## Methodology
1. Search for hardcoded string literals not using AppStrings
2. Search for hardcoded numeric values not using AppConstants
3. Search for hardcoded color values not using semantic tokens from Color+Extensions
4. Document findings with file paths and line numbers
5. Provide recommendations for refactoring

## Current State Assessment
Based on initial review of Design/design-tokens.md and related files:
- Design tokens are well-defined in markdown format
- Semantic color tokens exist in Color+Extensions.swift
- Font tokens exist in Font+Extensions.swift
- Some layout constants exist in AppConstants.swift
- String constants exist in AppStrings.swift

## Files Analyzed
All .swift files in the CoffeeCall module (excluding tests):
- Component files: ActivityCardView.swift, DriftCard.swift, InterestCard.swift, etc.
- Screen files: DiscoveryScreen.swift, DriftDetailScreen.swift, etc.
- ViewModel files: DiscoveryViewModel.swift, DriftDetailViewModel.swift, etc.
- Model files: Drift.swift, Activity.swift, etc.
- DesignSystem files: Color+Extensions.swift, Font+Extensions.swift, etc.
- App files: Coffee_CallApp.swift, ContentView.swift, etc.

## Findings Summary

### Hardcoded Strings Needing Migration to AppStrings
1. **DiscoveryViewModel.swift**:
   - Line 8: `@Published var selectedCategory: String = "Coffee"` - should use AppStrings.Discovery.Categories.coffee
   - Line 12: `let userName = "Robin"` - mock data
   - Line 13: `let userInitials = "RO"` - mock data
   - Lines 36-41: Various RadarPerson initials ("DK", "MR", "LM", "NP", "TH") - mock data
   - Lines 46-49: InterestCategory labels ("Coffee", "Walk", "Movie", "Food") - should use AppStrings.Discovery.Categories.*

2. **Other Files** (from grep analysis):
   - Various title strings in preview/mock data
   - "•" separator characters used frequently
   - Status strings like "STARTING SOON", "View all", "Past Drifts"
   - Image URL strings like "host_arjun"

### Hardcoded Numbers Needing Migration to AppConstants
1. **DiscoveryViewModel.swift**:
   - Lines 36-41: RadarPerson distance values (0.35, 0.45, 0.65, 0.75, 0.25, 0.55) - mock data
   - Lines 36-41: RadarPerson angle values (160, 30, 120, 210, 280, 330) - mock data

2. **AnimationSystem.swift** (these are actually appropriate as they're animation constants):
   - Lines 11, 14, 17, 21, 24, 27-29: Animation response/damping/duration values
   - Line 35: Press scale value (0.96)

3. **Various Files**:
   - Padding values: 24, 16, 12, 8, 4, 20, 40
   - Spacing values: 16, 4, 12, 8, 6, 20
   - Corner radius values: 40, 24, 20, 16, 32
   - Font sizes: 10, 12, 13, 14, 16, 18, 20, 22
   - Opacities: 0.04, 0.05, 0.06, 0.09, 0.1, 0.12, 0.15, 0.18, 0.2, 0.25, 0.3, 0.4, 0.9, 0.92
   - Scale effects: 0.9, 0.90, 0.96, 1.18
   - Kerning values: 1.0, 1.2, 1.8
   - Frame dimensions: 42, 48, 56, 64, 72, 90, 100, 130
   - Offsets: 8, 10, 24

### Hardcoded Colors Needing Migration to Semantic Tokens
1. **Color+Extensions.swift**:
   - Line 16: `static let darkOverlay = Color(red: 36/255, green: 52/255, blue: 71/255)` - should be a semantic token
   - Line 25: `static let statusError = Color(red: 0.87, green: 0.27, blue: 0.27)` - should be a semantic token

2. **Various Files**:
   - Direct use of `.black`, `.white` with opacities (should use textPrimary/textSecondary with opacity)
   - Direct RGB values in some places
   - Some direct color literals not using the semantic tokens

## Priority Ranking

### High Priority (Should be moved immediately)
1. Mock data in DiscoveryViewModel.swift - affects development/testing consistency
2. Hardcoded color values in Color+Extensions.swift - defeats the purpose of semantic tokens
3. Frequently used padding/spacing values that appear in AppConstants already but are ignored

### Medium Priority
1. Animation values that are already well-organized in AnimationSystem.swift but not consistently used
2. Font sizes and weights that could use the Font+Extensions helpers
3. Corner radius values that have equivalents in design tokens

### Low Priority
1. One-off values used only once or twice
2. Values that are already reasonably encapsulated
3. Truly constant values that are unlikely to change

## Recommended Constant Names and Locations

### For AppStrings.swift
Add to Discovery section:
```swift
static let defaultCategory = "Coffee"
```

Add mock data constants or move mock data to separate file

### For AppConstants.swift
Add sections for:
- MockRadarDistances: [0.35, 0.45, 0.65, 0.75, 0.25, 0.55]
- MockRadarAngles: [160, 30, 120, 210, 280, 330]
- Common paddings/spacings (though many already exist)
- Common opacities as Double values
- Common scale factors

### For Color+Extensions.swift
Add semantic tokens:
- `static let darkOverlay = Color("coffeeDarkOverlay")` (with asset)
- `static let statusError = Color("coffeeStatusError")` (with asset)
Or keep as computed values but with better naming

## Next Steps
1. Create backups of files to be modified
2. Extract mock data from DiscoveryViewModel.swift to constants or separate mock data file
3. Add missing semantic color tokens to Color+Extensions.swift
4. Review and consolidate frequently used numeric values into AppConstants.swift
5. Update all files to use the new constants
6. Verify all changes work correctly