rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // GLOBAL OVERRIDE: Allow all operations for signed-in users temporarily
    // This solves the Bundle ID mismatch causing erratic rule evaluation
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
